<!DOCTYPE html>
<html lang='en'>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Great Number Game</title>
    </head>
    <body>
        <div class='container'>

            <h1>
                The Great Number Game!
            </h1>
            
            <h3>
                number between 1 and 100!
            </h3>
            <form  action="/guess" method="POST">
                <input type = "number" name = "guess">
                <input type = "submit" value = "submit">
            </form>
            <% if(context.status == "alto"){ %>
                <div class="messages" >
                    <div class = "holdMessage">
                        <h2>
                            Too <%=context.status%> Numero mas bajo
                        </h2>
                    </div>
                </div>
            <% } 
            else if(context.status == "nada"){ %>
                <div class="messages">
                    <div class = "holdMessage">
                        <p>
                            Status: <%=context.status%>
                        </p>
                    </div>
                </div>
            <% } 
            else if(context.status == "Gano"){ %>
                <div class="messages" >
                    <div class = "holdMessage">
                        <h4>
                            You <%=context.status%>!
                        </h4>
                        <form action="/reset" method="POST">
                            <input type="submit" name="reset" value="Play Again">
                        </form>
                    </div>
                </div>
            <% } 
            else if(context.status == "bajo"){ %>
                <div class="messages">
                    <div class = "holdMessage">
                        <h4>Too <%=context.status%>!! Numero mas alto</h4>
                    </div>
                </div>
            <% } %>
        </div>
    </body>
</html>